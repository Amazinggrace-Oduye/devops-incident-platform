NODE_ENV=development
PORT=3000

# PostgreSQL — match these values in your docker-compose.yml
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=dip
DB_PASSWORD=dip
DB_DATABASE=devops_incident_platform
DB_LOGGING=false

# Auth
JWT_SECRET=dev-only-change-me
JWT_EXPIRES_IN=1d

# HTTP
CORS_ORIGIN=*
